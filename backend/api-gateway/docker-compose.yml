services:
  api-gateway:
    build: .
    image: api-gateway-service
    container_name: api-gateway-service
    ports:
      - "9095:9095"
    env_file:
      - .env
    volumes:
      - ./logs:/logs

networks:
  default:
    external: true
    name: microservices-network